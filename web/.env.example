# Pinata JWT for pinning record photos to IPFS. Optional: without it the
# app works, but the photo field reports that uploads are not configured.
# Needs the org:files:write scope (app.pinata.cloud > API Keys).
PINATA_JWT=
